package OopsByKK.streamAPI.Comparators;

public class Theory {

    public static void main(String[] args) {

        //Comparator means external class / logic that defines how two objects should be compared.
        //It’s like saying:
        //"I want to sometimes sort Student by name, sometimes by marks, etc."
        //You create a separate class (or anonymous class or lambda) that implements Comparator.

//public interface Comparator<T> {
//    int compare(T o1, T o2);
//}
//Same idea:
//0 → equal
//Negative → first smaller
//Positive → first greater
    }
}
